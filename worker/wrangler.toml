name = "sakib-assistant"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Public Firebase project settings (safe to commit — these identify the
# project, they don't grant access on their own; Firebase security comes
# from Auth + Firestore rules, not from hiding these).
[vars]
FIREBASE_PROJECT_ID = "application-tracker-3b675"
FIREBASE_WEB_API_KEY = "AIzaSyAH3sU1-b3sIlB4BGKt0T3QBvsOSnkKSBk"
ALLOWED_ORIGINS = "https://ahmedsakib1113.github.io,http://localhost:8791,http://127.0.0.1:8791"

# GEMINI_API_KEY is NOT set here — it's a secret. Set it after `wrangler login` with:
#   wrangler secret put GEMINI_API_KEY
# (paste your key from https://aistudio.google.com/apikey when prompted)
